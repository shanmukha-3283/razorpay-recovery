{
  "name": "razorpay-recovery",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:up": "docker compose up -d db redis",
    "db:down": "docker compose down",
    "db:setup": "pnpm --filter @razorpay-recovery/api db:setup",
    "demo": "node scripts/demo.mjs"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0"
}
